---
  - name : This is a demo on variables 
    hosts: all
    become: yes
    vars:
      user: alice
    tasks:
       - name : create a user
         user:
           name : "{{ user }}"
           state: present

         
       