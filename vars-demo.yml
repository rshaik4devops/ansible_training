---
  - name : This is a demo on variables 
    hosts: localhost
    become: yes
    vars:
      users:
        - ram
        - krishna
        - ramesh 

    tasks:
       - name: print 1st user
         debug:
           msg: the first user in list is "{{ users[0] }}"
       - name: print second user
         debug:
            msg: the second user from the list is {{ users[1] }}


         
       