---
  - name : This is a demo on variables 
    hosts: localhost
    become: yes
    vars:
      fruits:
        - ram
        - krishna
        - ramesh 

    tasks:
       - name: print 1st user
         debug:
           msg: the first user in list is {{ fruits[0] }}
       - name: print second user
         debug:
            msg: the second user from the list is {{ fruits[1] }}
       - name: print 3rd user 
         debug:
            msg: the third  user from the list is {{ fruits[2] }}
       


         
       